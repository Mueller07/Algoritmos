function calculaArea(base, altura){ //parametro
    const area = base * altura
    console.log(area)
}

calculaArea(3,5) // argumento
calculaArea(9,11)
calculaArea(45,59)
calculaArea(300,5)

// as variaveis criadas no parenteses = paramentos
//valores que passamos para estas variaveis = argumento